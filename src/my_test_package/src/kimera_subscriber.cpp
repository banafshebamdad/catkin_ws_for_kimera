// #include <ros/ros.h>
// #include <std_msgs/String.h>
#include <iostream>

// void topicCallback(const std_msgs::String::ConstPtr& msg) {
//     ROS_INFO("Receiced message: %s", msg->data.c_str);
// }

int main(int argc, char** argv) {
    // ros::init(argc, argv, "kimera_subscriber");
    // ros::NodeHandle nh;

    // ros::Subscriber sub = nh.subscribe("/kimera_vio_ros/imu_bias", 10, topicCallback);
    // ros::spin();
    std::cout << "\n --- Je suis encore ici. ---\n" << std::endl;
    return 0;
}